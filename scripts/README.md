# Scripts
Training and evaluation entrypoints:
- `train.py` – fine-tune BERT on SST-2 (Phase 4)
- `eval_mc_dropout.py` – run MC Dropout inference, save μ and σ² (Phase 5)
- `run_data_demo.py` – verify data pipeline (Phase 2)
